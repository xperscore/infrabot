apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/xperscore/infrabot:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/xperscore/infrabot:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: infrabot
        chartPath: charts/infrabot
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/xperscore/infrabot'
          image.tag: '{{.DIGEST_HEX}}'
