apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: {{.DOCKER_REGISTRY}}/xperscore/infrabot
    kaniko:
      buildContext:
        localDir: .
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/xperscore/infrabot:{{.VERSION}}'
deploy:
  kubectl: {}